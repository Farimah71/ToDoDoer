import "./header.scss";
import Logo from "../../components/logo/Logo";

const Header = () => {
  return (
    <header>
      <div className="header">
        <Logo />
      </div>
    </header>
  );
};

export default Header;
