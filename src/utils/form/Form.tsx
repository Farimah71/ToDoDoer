import MyButton from "./../../components/buttons/MyButton";

const Form = () => {
  //   const {
  //     register,
  //     handleSubmit,
  //     formState: { errors, isSubmitting, isSubmitSuccessful, submitCount },
  //   } = useForm<FormValues>();

  // const onSubmit = (data: FormValues) => {
  //   console.log(data);
  // };

  return (
    <>
      <form>
        {/* {isSubmitting && <div>Submitting...</div>}
      {submitCount > 0 && isSubmitSuccessful && (
        <div className="text-success">Task added successfully.</div>
      )} */}
      </form>
      {/* <div className="form-actions">
        <MyButton text="Save" type="submit" onClick={handleSubmit(onSubmit)} />
      </div> */}
    </>
  );
};

export default Form;
